{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type":"lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/reshaper.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build"
        },
    ]
}